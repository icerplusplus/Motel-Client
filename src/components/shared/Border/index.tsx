import { View, Text } from "react-native";
import React from "react";

type Props = {};

export const Border = (props: Props) => {
  return <View className="w-full border-[.4px] border-slate-300" />;
};
